export interface IUserLogin {
  email:string;
  password:string;
}

//we didn't write any exclamation in the above 2 properties of the interface
//because all the properties of an interface are required by default
