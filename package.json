{
    "name": "shopping-gallery",
    "version": "1.0.0",
    "engines": {
        "node": ">=18 <19" //since the latest stable node version is 18, also the space is important here
    },
    "scripts": {
        "start": "cd backend/built && node server.js",
        "prebuild": "cd backend && npm run build",
        "build": "cd frontend && npm run build"
    }
}